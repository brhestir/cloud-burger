<h1>Burgers!</h1>

<h2>Burgers that are devoured!</h2>

<ul>
  {{#each burgers}}
    {{#unless devoury}}
      {{> burgers/burger-block devour=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Burgers that are devoury!</h2>

<ul>
  {{#each burgers}}
    {{#if devoury}}
      {{> burgers/burger-block devour=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="ca" name="burger_name">
  </div>

  <div class="form-group">
    <label for="devoury">Devoury?</label><br>
    <input type="radio" name="devoury" value="1" checked> Devoury!<br>
    <input type="radio" name="devoury" value="0"> Not Devoury
  </div>

  <button type="submit">Add Burger</button>
</form>
